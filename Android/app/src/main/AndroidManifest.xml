<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- IME service -->
        <service
            android:name=".SimeService"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Launcher / setup activity. NoActionBar so we can render
             our own title inside the layout. -->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Editor for adding a quick phrase, launched from the IME's
             quick-phrase panel via "+" button. -->
        <activity
            android:name=".QuickPhraseEditActivity"
            android:label="添加常用语"
            android:exported="false" />

    </application>

</manifest>
